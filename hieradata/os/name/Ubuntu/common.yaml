---
include:
  classes:
    - apt
    - profiles::apt
    
    # MOTD
    - profiles::motd

    # NTP
    - ntp

    # Debian resolvconf module
    - resolvconf

    # SSH
    - profiles::ssh

    # Sudo
    - sudo
    - profiles::sudo

    # System
    - profiles::system::hardening
